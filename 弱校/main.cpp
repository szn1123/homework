#include <iostream>
#include <cstdio>
#include <cmath>
using namespace std;

int main()
{
    int n,m,k;
    long long ans=0;
    while(scanf("%d%d%d",&n,&m,&k)!=EOF)
    {
        if(k<4) ans=0;
        else
        {
            for(int i=1;i<)
        }
    }

    return 0;
}
